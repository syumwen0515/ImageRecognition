fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
Pillow>=10.0.0
pytesseract>=0.3.10
# headless variant: no GUI libs needed on a server (avoids libGL/libSM errors)
opencv-python-headless>=4.8.0
sqlalchemy>=2.0.0
aiofiles>=23.0.0
python-dotenv>=1.0.0
numpy>=1.24.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
# PostgreSQL driver (required when DATABASE_URL points to PostgreSQL on Render)
psycopg2-binary>=2.9.0
# Cloud image storage
cloudinary>=1.36.0
# GPU-accelerated OCR — disabled by default for Render free tier (512 MB RAM limit).
# Re-enable locally or on a paid plan with enough memory:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install easyocr
# easyocr>=1.7.1
# Optional: Claude Vision API (cloud, highest accuracy)
# anthropic>=0.40.0
# Optional: Google Cloud Vision API (cloud)
# google-cloud-vision>=3.4.0
